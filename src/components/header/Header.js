import {HeaderTop} from "./HeaderTop";
import { Navigation } from "./Navigation";

export const Header = () => (
    <header className="header"> 
        <HeaderTop/>
        <Navigation/>
    </header>
)